- name: Debug AWS Secrets
  run: |
    echo "AWS KEY LENGTH: ${#AWS_ACCESS_KEY_ID}"
    echo "AWS SECRET LENGTH: ${#AWS_SECRET_ACCESS_KEY}"
  env:
    AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
    AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
